# Tenancy Module

Scope:

- tenant resolution by host/domain
- tenant lifecycle and status
- tenant branding metadata
- custom domain mapping

Dependencies:

- iam for session/user context

Provides:

- resolved tenant context for downstream modules
